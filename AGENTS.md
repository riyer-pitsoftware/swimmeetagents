# Agent Instructions

## Issue Tracking

This project uses **bd (beads)** for issue tracking.
Run `bd prime` for workflow context.

Quick reference:
- `bd ready`
- `bd show <id>`
- `bd update <id> --status in_progress`
- `bd close <id>`
- `bd sync`

Always create/update beads for significant deliverables and blockers.
